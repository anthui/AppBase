# 打应用市场包 全部走这里
# 打包命令 gradlew clean assembleAppReleaseChannels -Dchannel=market -Dhost=api -Dis_open_log=false

#Release打包 gradlew clean assembleAppReleaseChannels -Dhost=api -Dis_open_log=false -PchannelList=bbz_guanwang_ad
#Debug打包 gradlew clean assembleAppDebugChannels -Dhost=api -Dis_open_log=false -PchannelList=bbz_guanwang_ad

# 输出渠道apk只有渠道名
# -Donly_channel_name=true
# 服务器环境：dev -> 测试；beta -> 预发； api -> 正式
# -Dhost=dev
# 使用哪个渠道文件配置
# -Dchannel=market
# 指定版本号
# -DversionCode = 5
# 指定版本名称
# -DversionName = "1.4.0"

bbz_360


