#**********************这里出包 仅提供测试使用**********************************

#测试域名 debug打包 gradlew clean assembleAppDebugChannels -Dchannel=developer -Dhost=dev

#测试域名 releases打包 gradlew clean assembleAppReleaseChannels -Dchannel=developer -Dhost=dev

#正式域名 debug打包 gradlew clean assembleAppDebugChannels -Dchannel=developer -Dhost=api

#正式域名 releases打包 gradlew clean assembleAppReleaseChannels -Dchannel=developer -Dhost=api

#正式域名 升级包专用 gradlew clean assembleAppReleaseChannels -Dchannel=developer -Dhost=api  -DversionCode=5 -DversionName="1.4.0"
#升级版本用  -DversionCode=5 -DversionName="1.4.0"

# 是否打开日志
# -Dis_open_log = true 默认true
# 服务器环境：dev -> 测试；beta -> 预发； api -> 正式
# -Dhost=dev
# 打包该文件下的渠道
# -Dchannel=developer

bbz_guanwang_ad      # 开发测试默认渠道

